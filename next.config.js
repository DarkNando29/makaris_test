/** @type {import('next').NextConfig} */
const nextConfig = {
  images: { 
    domains: [
      'instagram.fsjo6-1.fna.fbcdn.net',
      'images.unsplash.com'
    ],
  },
};

module.exports = nextConfig;
